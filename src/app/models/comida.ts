export interface Comida{
    id?: String; //el ? indica que es una propiedad opcional
    name?: String;
    description?: String;
    price?: number;
    image?: String;
    disponibilidad?: boolean;
    tipo?:String;


}